<Window x:Class="Cybersecurity_Chatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberBot - Cybersecurity Awareness Assistant" Height="700" Width="900"
        Background="#1E1E2E" ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="BotMessage">
            <Setter Property="Foreground" Value="#00E5FF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="UserMessage">
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Chat display -->
            <RowDefinition Height="Auto"/>
            <!-- Input area -->
        </Grid.RowDefinitions>

        <!-- HEADER with ASCII art + subtitle -->
        <Border Grid.Row="0" Background="#2D2D44" Padding="10">
            <StackPanel>
                <TextBlock x:Name="AsciiHeader" FontFamily="Consolas" FontSize="16" 
                           Foreground="#00E5FF" TextAlignment="Center" 
                           Text="Loading..."/>
                <TextBlock Text="🛡️ Cybersecurity Awareness Assistant — Protecting South Africa Online 🛡️"
                           Foreground="#00FF7F" FontSize="14" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Conversation display -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="ChatDisplay" TextWrapping="Wrap" Margin="10"
                       Foreground="White" FontSize="13"
                       Background="Transparent"/>
        </ScrollViewer>

        <!-- Input area -->
        <Border Grid.Row="2" Background="#2D2D44" Padding="10">
            <DockPanel>
                <Button x:Name="SendButton" Content="Send" DockPanel.Dock="Right"
                        Width="80" Height="30" Background="#00E5FF" Foreground="Black"
                        FontWeight="Bold" Click="SendButton_Click"/>
                <TextBox x:Name="UserInput" DockPanel.Dock="Left" 
                         Background="#3E3E5C" Foreground="White" 
                         CaretBrush="White" FontSize="14"
                         KeyDown="UserInput_KeyDown"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>